# Practice project for react + typescript

This is a simple todo application done using React (using typescript) and Tailwind css

# Streps to run the project

clone the repository
run `npm install`
run `npm start`
Enjoy!
